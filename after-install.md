# Hiding installed

Enable it if you did not install with `--enable`:

```bash
hermes plugins enable hiding
```

Restart Hermes or the gateway after enabling.

Commands:

- `/hiding [file|description]` — Strip AI leakage from files

Bundled skills are available as `hiding:hiding`.
