---
name: Bug report
about: The skill misbehaved — wrong stripping, broken file, unexpected output
labels: bug
---

**Agent / platform**
(e.g. Claude Code 2.x, Cursor, Codex — and how you installed: `npx skills` or `/plugin install`)

**Command used**
(e.g. `/hiding --mode backup config.yml`)

**What happened**

**What you expected**

**Sample input** (redact any real credentials — replace with `FAKE_KEY_123`)
